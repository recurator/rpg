<%= form_with(model: character_type, local: true) do |form| %>
  <% if character_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(character_type.errors.count, "error") %> prohibited this character_type from being saved:</h2>

      <ul>
        <% character_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :avatar %>
    <%= form.file_field :avatar %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <ul>
    <%= form.fields_for :character_attributes do |subform| %>
      <li>
        <div class="field">
          <%= subform.label :icon %>
          <%= subform.file_field :icon %>
        </div>

        <div class="field">
          <%= subform.label :name %>
          <%= subform.text_field :name %>
        </div>

        <div class="field">
          <%= subform.label :value %>
          <%= subform.text_field :value %>
        </div>
      </li>
    <% end %>
  </ul>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
